# Copy to infra/.env and adjust for your local environment.
# In production these values come from Azure App Service config / Key Vault.

POSTGRES_USER=brewery
POSTGRES_PASSWORD=brewery
POSTGRES_DB=brewery

# DATABASE_URL is constructed from the above for the backend service.
DATABASE_URL=postgresql+psycopg://brewery:brewery@db:5432/brewery
